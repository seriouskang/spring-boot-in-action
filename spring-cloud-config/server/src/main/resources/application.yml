management:
  endpoints:
    web:
      exposure:
        include: "*"

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/seriouskang/spring-cloud-config-tutorial-repo